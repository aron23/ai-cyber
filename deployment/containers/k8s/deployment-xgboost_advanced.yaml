apiVersion: apps/v1
kind: Deployment
metadata:
  name: spam-filter-xgboost-advanced
  namespace: spam-filter
  labels:
    app: spam-filter-xgboost-advanced
    model-type: baseline
    f1-capability: "89"
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: spam-filter-xgboost-advanced
  template:
    metadata:
      labels:
        app: spam-filter-xgboost-advanced
        model-type: baseline
        f1-capability: "89"
    spec:
      containers:
      - name: spam-filter-xgboost-advanced
        image: spam-filter/xgboost_advanced:1.0.0
        ports:
        - containerPort: 8000
          name: http
        env:
        - name: MODEL_NAME
          value: "xgboost_advanced"
        - name: MODEL_VERSION
          value: "1.0.0"
        - name: MODEL_TYPE
          value: "baseline"
        - name: F1_CAPABILITY
          value: "89.04"
        envFrom:
        - configMapRef:
            name: spam-filter-config
        resources:
          requests:
            memory: "2Gi"
            cpu: "1"
          limits:
            memory: "4g"
            cpu: "2"
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 15
          periodSeconds: 15
          timeoutSeconds: 5
          failureThreshold: 2
        volumeMounts:
        - name: model-storage
          mountPath: /app/models
          readOnly: true
        - name: logs
          mountPath: /app/logs
      volumes:
      - name: model-storage
        persistentVolumeClaim:
          claimName: model-storage-pvc
      - name: logs
        emptyDir: {}
      nodeSelector:
        workload-type: cpu-intensive
      tolerations:
      - key: "workload-type"
        operator: "Equal"
        value: "cpu-intensive"
        effect: "NoSchedule"
---
apiVersion: v1
kind: Service
metadata:
  name: spam-filter-xgboost-advanced-service
  namespace: spam-filter
  labels:
    app: spam-filter-xgboost-advanced
spec:
  selector:
    app: spam-filter-xgboost-advanced
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  type: ClusterIP
